\begin{table}[htbp]
\centering
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~91; SAC-MPC uses agent~51. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~32; SAC-MPC uses agent~53. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~32; SAC-MPC uses agent~51. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~32; SAC-MPC uses agent~50. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~93; SAC-MPC uses agent~91. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~51; SAC-MPC uses agent~32. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~50; SAC-MPC uses agent~32. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~53; SAC-MPC uses agent~32. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~53; SAC-MPC uses agent~93. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~91; SAC-MPC uses agent~52. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~32; SAC-MPC uses agent~49. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~32; SAC-MPC uses agent~53. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~32; SAC-MPC uses agent~51. Values in parentheses show the percentage change relative to the No Control baseline.}
\caption{Mean $\pm$ standard deviation of Total Time Spent (TTS, veh$\cdot$h) across all experimental runs for each controller and scenario. DDPG-MPC uses agent~93; SAC-MPC uses agent~\label{tab:tts_comparison_agent}
\begin{tabular}{lcccc}
\toprule
Controller & Scenario 1 & Scenario 2 & Scenario 3 & Scenario 4 \\
\midrule
No~Control & $2754.9$ & $2748.0 \pm 25.1$ & $2754.9$ & $2748.0 \pm 25.1$ \\
PI-ALINEA & $2587.5 \pm 1.8$ ($-6.1\%$) & $2622.6 \pm 29.0$ ($-4.6\%$) & $2710.6 \pm 24.6$ ($-1.6\%$) & $2692.1 \pm 36.4$ ($-2.0\%$) \\
Hier.~MPC & $2453.7 \pm 0.6$ ($-10.9\%$) & $2450.5 \pm 24.7$ ($-10.8\%$) & $2648.6 \pm 17.7$ ($-3.9\%$) & $2650.8 \pm 27.0$ ($-3.5\%$) \\
DDPG-MPC & $2616.6 \pm 3.9$ ($-5.0\%$) & $2616.6 \pm 24.0$ ($-4.8\%$) & $2680.2 \pm 2.9$ ($-2.7\%$) & $2666.9 \pm 26.5$ ($-3.0\%$) \\
SAC-MPC & $2697.4 \pm 44.3$ ($-2.1\%$) & $2676.3 \pm 17.3$ ($-2.6\%$) & $2891.5 \pm 118.0$ ($+5.0\%$) & $2940.4 \pm 182.3$ ($+7.0\%$) \\
\bottomrule
\end{tabular}
\end{table}
